package spaceSimStuff;

import java.awt.Color;
import java.math.BigInteger;

public class PlanetTestBench {
/*
	public static void test() {
		//This tester would normally be a main() but I did not want to create a new package so I call this in main
		Planet testPlanet = new Planet (0,0,1,0,100,new BigInteger("10"),Color.blue, 0, spaceSystem);
		Planet[] testSystem = new Planet[1];
		testSystem[0] = testPlanet;
		int size = 1;
		
		//Test updateAccVel and updatePos (I don't know how to test these seperatly)
		testPlanet.updateAccVel(0,testSystem,size,1);
		testPlanet.updatePos(1);
		if(testPlanet.getX() != 1) {
			System.out.println("FAILED planet updateAccelVel/updatePos");
		}
		
		//Test getY
		if(testPlanet.getY() != 0)
			System.out.println("FAILED planet getY");
		
		//Test getPlanetRadius
		if(testPlanet.getPlanetRadius() != 100)
			System.out.println("FAILED planet getPlanetRadius");
		
		//Test getColor
		if(testPlanet.getColor() != Color.blue)
			System.out.println("FAILED planet getColor");
		
		//Test setMass
		testPlanet.setMass(new BigInteger("120"));
		
		System.out.println("PlanetTestBench completed all tests");
		System.out.println();
		
	}
	*/
}
